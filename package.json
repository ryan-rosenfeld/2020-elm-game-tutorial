{
  "scripts": {
    "serve": "rm -rf target/ && mkdir target && ln -s ../src/index.html target/index.html && elm-live --path-to-elm=./node_modules/.bin/elm --dir=./target --hot --open -- src/Main.elm --output=./target/dist.js"
  },
  "dependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.4",
    "elm-live": "^4.0.2"
  }
}
