{
  "scripts": {
    "serve": "elm-live --path-to-elm=./node_modules/.bin/elm --hot --open -- src/Main.elm --output=dist.js"
  },
  "dependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.4",
    "elm-live": "^4.0.2"
  }
}
